{
  "//": "Belt and braces. In a Manifest repo the CDK project root is infra/, so nothing here is ever imported as a resource. In a project whose CDK root is the repo root, the loader imports every .ts EXCEPT directories carrying a package.json - without this file, `cdk plan` would import collect/calls.ts and run it against the live API on every plan.",
  "name": "call-capture-scripts",
  "private": true,
  "type": "module"
}
