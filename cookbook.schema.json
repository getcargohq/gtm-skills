{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/getcargohq/cargo-cookbooks/cookbook.schema.json",
  "title": "cookbook.json",
  "description": "The data behind one cookbook: what it produces, what it needs answered before it can deploy, and how you know it worked. The prose lives in README.md and SKILL.md; this file is what the installer skill, the validator, and the UI deployment surface all read. It deliberately does NOT carry `requires` — the dependency graph lives once, in cargo.scaffold.json.",
  "type": "object",
  "required": ["slug", "kind", "outcome", "state"],
  "additionalProperties": false,
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Must equal the folder name."
    },
    "kind": {
      "enum": ["outcome", "foundation"],
      "description": "outcome = a use case somebody installs on purpose, and it carries a SKILL.md. foundation = a slot other cookbooks build on (base-gtm, crm-sync); it defines no motion of its own and carries no SKILL.md."
    },
    "outcome": {
      "type": "string",
      "minLength": 20,
      "description": "One line, in the words a user would say. Rendered into the README menu, llms.txt, and the SKILL.md description."
    },
    "chain": {
      "type": ["integer", "null"],
      "minimum": 0,
      "maximum": 10,
      "description": "Step in the cookbook chain (Notion B-Q4), or null when the cookbook sits outside it."
    },
    "state": {
      "enum": ["to-be-approved", "approved"],
      "description": "Approved requires BOTH a fresh-workspace test and two customer or partner implementations. Nothing may claim a public outcome for a cookbook that is not approved."
    },
    "approval": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "demoWorkspace": {
          "type": ["string", "null"],
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
          "description": "Date the fresh-demo-workspace deployment passed every doneWhen check."
        },
        "implementations": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["customer", "date"],
            "additionalProperties": false,
            "properties": {
              "customer": { "type": "string" },
              "date": {
                "type": "string",
                "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
              },
              "workspaceUuid": { "type": "string" },
              "note": { "type": "string" }
            }
          }
        }
      }
    },
    "inputs": {
      "type": "array",
      "description": "Everything that must be answered before this cookbook can deploy. Derive before you ask: an input carrying a `derive` is looked up, not asked. If more than about four inputs have no `derive`, the interview is too long and most of them should have been lookups.",
      "items": {
        "type": "object",
        "required": ["id", "kind", "ask"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9]*$" },
          "kind": {
            "enum": ["value", "generated", "env", "manual"],
            "description": "value = patch it into file/path. generated = the skill writes the whole file from the answers. env = a secret the operator sets in .env before deploy; the skill never reads or writes it. manual = something that must be true outside the repo (a CRM property, an OAuth connection) and can only be checked, not written."
          },
          "ask": {
            "type": "string",
            "description": "The question, in plain words, asked only when `derive` fails or is absent."
          },
          "why": {
            "type": "string",
            "description": "What breaks if this is wrong. Shown when the operator pushes back on a question."
          },
          "file": {
            "type": "string",
            "description": "Repo-relative. May live in a required sibling folder."
          },
          "path": {
            "type": "string",
            "description": "Dotted path into the exported config, e.g. config.urls."
          },
          "env": {
            "type": "string",
            "description": "Environment variable name, for kind: env."
          },
          "type": {
            "enum": ["string", "string[]", "number", "boolean", "markdown"]
          },
          "default": {},
          "max": { "type": "number" },
          "derive": {
            "type": "string",
            "description": "How to look the answer up instead of asking: a cargo-ai call, an existing connector, a row count."
          },
          "validate": {
            "type": "string",
            "description": "The check that says the answer is usable, in words the skill can act on."
          }
        }
      }
    },
    "doneWhen": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "The acceptance test, one checkable statement per line. This is what a fresh-workspace run walks to earn `approval.demoWorkspace`."
    },
    "cost": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "note": {
          "type": "string",
          "description": "Where the money actually goes, and what controls it."
        },
        "scalesWith": {
          "type": "string",
          "description": "The thing that multiplies the bill."
        }
      }
    },
    "composesInto": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Cookbook slugs that build on this one. Suggested as the next step after a successful deploy."
    }
  }
}
