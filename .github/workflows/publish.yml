name: Publish to ClawHub

# GitHub is where these live; the registry is where they are found. A skill that
# only exists as a repo is discoverable by people who already know to look for
# it — which is the opposite of the point.
#
# Directories are discovered from */SKILL.md rather than hand-listed. The pack
# learned that the hard way: its list fell two skills behind the tree and nobody
# noticed until someone counted.

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry_run:
        description: Resolve and print, but do not publish
        type: boolean
        required: false
        default: false

permissions:
  contents: read

concurrency:
  group: clawhub-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      CLAWHUB_OWNER: cargo-ai
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      # Never publish an unvalidated skill: the registry is the one place a
      # stale credit price reaches someone with no way to know it is wrong.
      - name: Validate before publishing
        run: node scripts/validate.ts

      - run: npm install -g clawhub

      - name: Authenticate
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
        run: |
          if [ -z "${CLAWHUB_TOKEN:-}" ]; then
            echo "::error::CLAWHUB_TOKEN secret is not set. Create an API token at https://clawhub.ai and add it as a repo secret."
            exit 1
          fi
          clawhub login --token "$CLAWHUB_TOKEN"
          clawhub whoami

      - name: Publish each skill
        env:
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -u
          published=0
          skipped=0
          failed=0

          for dir in */; do
            dir="${dir%/}"
            [ -f "$dir/SKILL.md" ] || continue

            version=$(awk -F'"' '/^version:/ {print $2; exit}' "$dir/SKILL.md")
            if [ -z "$version" ]; then
              echo "::error::No version: in $dir/SKILL.md"
              failed=$((failed + 1))
              continue
            fi

            echo "::group::$dir@$version"

            if [ "$DRY_RUN" = "true" ]; then
              echo "[dry-run] clawhub skill publish ./$dir --version $version --owner $CLAWHUB_OWNER"
              echo "::endgroup::"
              continue
            fi

            if output=$(clawhub skill publish "./$dir" \
                  --version "$version" \
                  --owner "$CLAWHUB_OWNER" 2>&1); then
              echo "$output"
              published=$((published + 1))
            elif echo "$output" | grep -qiE "already (exists|published)|version .* (exists|taken)"; then
              echo "Skipped — $dir@$version already published"
              skipped=$((skipped + 1))
            else
              echo "::error::Publish failed for $dir@$version"
              echo "$output"
              failed=$((failed + 1))
            fi

            echo "::endgroup::"
          done

          echo "Published: $published, Skipped: $skipped, Failed: $failed"
          [ "$failed" -eq 0 ]
