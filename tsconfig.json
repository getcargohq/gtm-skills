{
  "//": "Typechecks every skill's example resources against the installed CDK package, from the repo root. Consumer projects still run `cargo-ai cdk types` after selecting live connectors. Nothing here ships: a consumer's tsconfig comes from `cargo-ai cdk init`.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": []
  },
  "include": ["*/**/*.ts", ".cargo-ai/**/*.d.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "scripts",
    "hooks",
    "assets",
    "evals",
    "*/apps/*/**",
    "*/workers/*/**"
  ]
}
