{
  "//": "Typechecks every skill's example resources against the CDK, from the repo root. Needs generated types: `cargo-ai login`, then `npm run types`. Nothing here ships: a consumer's tsconfig comes from `cargo-ai cdk init --template blank`.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": []
  },
  "include": ["**/*.ts", ".cargo-ai/**/*.d.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "scripts",
    "hooks",
    "assets",
    "evals",
    "*/apps/*/**",
    "*/workers/*/**"
  ]
}
