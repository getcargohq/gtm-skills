{
  "slug": "account-scoring",
  "kind": "outcome",
  "outcome": "Every account scored and tiered against your written ICP by an agent that cites its evidence, with the rationale on the CRM record",
  "chain": 1,
  "state": "to-be-approved",
  "approval": {
    "demoWorkspace": null,
    "implementations": []
  },
  "inputs": [
    {
      "id": "icp",
      "kind": "generated",
      "file": "gtm-knowledge-graph/context/icp.md",
      "type": "markdown",
      "ask": "Who do you sell to, and who do you refuse? Name the fit signals and, just as important, the disqualifiers",
      "why": "The criteria ARE the prompt. There are no point weights in code, so a vague ICP produces vague scores and the disqualifiers are what cap a bad account at 20.",
      "derive": "If the workspace already has closed-won and closed-lost data, extract the industry, size, geography and stack that separate won from lost accounts and propose the ICP from it rather than asking cold.",
      "validate": "the file names at least one disqualifier, not only fit signals"
    },
    {
      "id": "languageModel",
      "kind": "value",
      "file": "account-scoring/agents/scorer.ts",
      "path": "languageModel",
      "type": "string",
      "default": "gpt-4o",
      "ask": "Which language model should judge the accounts?",
      "why": "Scoring quality and per-account cost both live here.",
      "derive": "whichever LLM connector is already authenticated in the workspace"
    },
    {
      "id": "crmScoreProperties",
      "kind": "manual",
      "ask": "Create cargo_score, cargo_tier, cargo_rationale and cargo_last_updated_at as properties on the CRM's account object",
      "why": "The play writes all four back. If they do not exist the run looks successful and the scores land nowhere, which is the failure mode that wastes a whole batch.",
      "derive": "read the CRM's account schema through the crm-sync connector and report which of the four are missing",
      "validate": "all four exist, and cargo_score plus cargo_tier are selected as columns on the accounts model so the tier segments can filter on them"
    },
    {
      "id": "tierThresholds",
      "kind": "value",
      "file": "account-scoring/segments/tiers.ts",
      "type": "string",
      "ask": "Which tiers do you want as segments, and at what score boundaries?",
      "why": "The agent emits A, B and C but the cookbook ships segments for A and C only. Tier B accounts land in no segment unless you add one.",
      "validate": "every tier the agent can emit is either covered by a segment or deliberately excluded"
    }
  ],
  "doneWhen": [
    "a test account run shows the agent's lookups in the trace, not a score guessed from the domain name",
    "the CRM record carries a score with a rationale citing real evidence and named ICP criteria",
    "the evaluator scores at least 0.8",
    "after the next model refresh the account appears in the matching tier segment"
  ],
  "cost": {
    "note": "One LLM call plus up to two Cargo database calls (matchBusiness, enrichBusinessFirmographics) per account, on arrival and again on the weekly stale sweep (last scored 3+ months ago).",
    "scalesWith": "accounts in the model, times re-scores. A TAM built by tam-building scores at that size."
  },
  "composesInto": ["routing-engine", "rep-cockpit", "ai-sdr"],
  "invariants": [
    {
      "holds": "The scorer looks the account up before judging it",
      "whatBreaks": "A score guessed from the domain name is unfalsifiable and the rationale cites nothing. The evaluator exists to fail exactly this, and a book scored that way is worse than an unscored one because people trust it.",
      "where": "account-scoring/agents/scorer.ts"
    },
    {
      "holds": "ICP disqualifiers cap the score, they are not just negative weight",
      "whatBreaks": "Without a cap, a disqualified account with many fit signals still scores high and reaches a rep. The disqualifiers are the half of an ICP that actually protects the team's time.",
      "where": "account-scoring/agents/scorer.ts"
    },
    {
      "holds": "`cargo_score` and `cargo_tier` are selected as columns on the accounts model",
      "whatBreaks": "The tier segments filter on a column they cannot see, so they come back empty while everything upstream reports success.",
      "where": "account-scoring/segments/tiers.ts"
    },
    {
      "holds": "Every tier the scorer can emit is either covered by a segment or excluded on purpose",
      "whatBreaks": "The shipped segments cover A and C. The agent also emits B, so tier B accounts land nowhere and quietly disappear from the book.",
      "where": "account-scoring/segments/tiers.ts"
    }
  ],
  "variations": [
    {
      "id": "deterministic-scoring",
      "when": "You need fixed cost and exact reproducibility, or an LLM judgement is not acceptable to your team",
      "how": "Swap the agent call for the native `scoring` node, with criteria as {name, value, score} booleans in the workflow",
      "trade": "The criteria move out of the ICP markdown and into code, so they stop being reviewable by non-engineers, and you lose the rationale entirely",
      "affects": [
        "account-scoring/agents/scorer.ts",
        "account-scoring/plays/score-accounts.ts"
      ]
    },
    {
      "id": "skip-crm-roundtrip",
      "when": "You want the score on the model directly and do not need it visible in the CRM",
      "how": "Write with the platform's `native.modelUpsert({...})` in the workflow instead of going through the CRM connector",
      "trade": "Reps lose the score and rationale where they actually work. The native's input is untyped, so confirm the field shape on the first run",
      "affects": ["account-scoring/plays/score-accounts.ts"]
    },
    {
      "id": "no-crm-at-all",
      "when": "You have no CRM, or you do not want to hand this cookbook a CRM credential",
      "how": "Take `skip-crm-roundtrip`, then drop the `crm-sync` requirement so no CRM connector is registered",
      "trade": "Every other CRM-dependent cookbook you install later will pull `crm-sync` back in anyway",
      "affects": ["crm-sync/connectors/hubspot.ts"]
    }
  ]
}
