{
  "//": "Belt and braces. In a Manifest repo the CDK project root is infra/, so nothing here is ever imported as a resource. In a project whose CDK root is the repo root, the loader imports every .ts EXCEPT directories carrying a package.json - without this file, `cdk plan` would import collect/week.ts and walk git/infra on every plan.",
  "name": "weekly-planning-scripts",
  "private": true,
  "type": "module"
}
