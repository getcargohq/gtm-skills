{
  "name": "gtm-skills",
  "private": true,
  "description": "Repo tooling only. Nothing here ships to a customer: one-off skills are installed with `npx skills add`, cookbooks with `cargo-ai cdk add cookbook/<name>`, and the CDK project shell comes from `cargo-ai cdk init`.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "validate": "node scripts/validate.ts && node scripts/check-pipelines.mjs && node scripts/generate-llms-txt.ts --check && node scripts/build-catalog.mjs --check",
    "validate:routing": "node .cargo-skills/.github/scripts/routing-eval.ts --skills-root . 2>/dev/null || node ../cargo-skills/.github/scripts/routing-eval.ts --skills-root .",
    "types": "cargo-cdk types",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.scripts.json",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@cargo-ai/cdk": "^1.0.66",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prettier": "^3.4.2",
    "typescript": "^5.3.2",
    "yaml": "^2.6.0"
  }
}
