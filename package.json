{
  "name": "gtm-skills",
  "private": true,
  "description": "Repo tooling only. Nothing here ships to a customer: skills are installed folder by folder with `npx skills add`, and the CDK project shell comes from `cargo-ai cdk init --template blank`.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "validate": "node scripts/validate.ts && node scripts/check-cookbooks.mjs && node scripts/generate-llms-txt.ts --check && node scripts/build-catalog.mjs --check",
    "validate:routing": "node .cargo-skills/.github/scripts/routing-eval.ts --skills-root . 2>/dev/null || node ../cargo-skills/.github/scripts/routing-eval.ts --skills-root .",
    "types": "cargo-cdk types",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@cargo-ai/cdk": "^1.0.51",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.3.2",
    "yaml": "^2.6.0"
  }
}
