{
  "name": "cargo-cookbooks",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "types": "cargo-cdk types",
    "typecheck": "tsc --noEmit",
    "plan": "cargo-cdk plan",
    "deploy": "cargo-cdk deploy",
    "validate": "node scripts/check-scaffold.mjs && node scripts/check-cookbooks.mjs && node scripts/routing-eval.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "postinstall": "npm run types || echo 'skipped: cargo-cdk types needs a workspace (run `cargo-ai login`, then `npm run types`). Typecheck works without it.'",
    "validate:routing": "node scripts/routing-eval.mjs"
  },
  "dependencies": {
    "@cargo-ai/cdk": "^1.0.28",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.3.2"
  }
}
